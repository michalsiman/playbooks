---
- name: Aktualizace systému
  hosts: linuxservers
  become: yes
  tasks:
    - name: Update balíčků (apt update)
      apt:
        update_cache: yes

    - name: Upgrade balíčků (apt upgrade)
      apt:
        upgrade: dist
